# Show draft documentation pages in the sidebar and allow direct URL access.
# Pages marked `draft: true` are hidden in production builds.
# VITE_SHOW_DRAFTS=true

# Public Builder key for the published community-app catalog. When unset, the
# docs app keeps the checked-in seed listing so local development still works.
# BUILDER_PUBLIC_KEY=
